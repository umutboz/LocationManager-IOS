# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LocationManagerTestApp' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LocationManagerTestApp
pod 'LocationManager' , :git => 'http://bellatrix:8080/tfs/ArgeMimariCollection/OneFrameIOS/_git/LocationManager', :tag => '1.0.0'
  target 'LocationManagerTestAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
